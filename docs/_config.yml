title: dsh-plugin-inspector
description: Know what a DeepSeek Harness plugin does before you install it. Static analysis that never runs the code.
theme: jekyll-theme-primer
show_downloads: false

defaults:
  - scope:
      path: ""
    values:
      layout: default
